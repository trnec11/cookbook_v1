import { BaseModel } from './base/base.model';
import { Recipe } from './schemas/recipe';

export class RecipeModel extends BaseModel<Recipe> {
    // here we can define some specific stuffs for recipe model
}